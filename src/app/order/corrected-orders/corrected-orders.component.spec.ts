/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { CorrectedOrdersComponent } from './corrected-orders.component';

describe('Component: CorrectedOrders', () => {
  it('should create an instance', () => {
    let component = new CorrectedOrdersComponent();
    expect(component).toBeTruthy();
  });
});
